// STEP5 - USER_ROLE enum created - [Enum stores non changing values(fixed values/properties) instead of classes can be part with dynamic db]

package com.eztrad.servercomp.domain;

public enum USER_ROLE {
    ROLE_ADMIN,
    ROLE_CUSTOMER
}
